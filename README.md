# Face recogniztion using Python and OpenCV

A simple notebook using `Python` and `OpenCV` to recognize images. Just check the self-explained `FacesRecognition.ipynb` file. 

This time, I used the **haarcascade** method.
